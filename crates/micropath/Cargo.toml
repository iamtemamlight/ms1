[package]
name = "micropath"
version = "0.1.0"
edition = "2021"
description = "No-hardware <5us deterministic fixed-point arbiter hot path (fused M1-M5 + gas)"

[[bin]]
name = "micropath-bench"
path = "src/bin/bench.rs"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1