apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-signer-access
  namespace: allbright-fleet
spec:
  podSelector:
    matchLabels:
      app: allbright-runner
  policyTypes:
  - Egress